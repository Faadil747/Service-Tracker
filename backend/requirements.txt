fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy[asyncio]>=2.0.0
alembic>=1.13.0
aiosqlite>=0.20.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-multipart>=0.0.9
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
httpx>=0.27.0
aiofiles>=23.2.1
python-dotenv>=1.0.0
email-validator>=2.1.0
# ── Production database drivers ──────────────────────────────
# MySQL (async) — used in production. asyncmy ships prebuilt wheels for Linux.
asyncmy>=0.2.9
# bcrypt is imported directly by auth_service (passlib pulls it in too, but pin it explicitly).
bcrypt>=4.0.1

